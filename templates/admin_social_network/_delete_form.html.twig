<form method="post" action="{{ path('app_admin_social_network_delete', {'id': social_nework.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social_nework.id) }}">
    <button class="btn btn-danger btn-round">Supprimer</button>
</form>
