{% extends 'baseAdmin.html.twig' %}

{% block title %}SocialNework{% endblock %}

{% block body %}
    <h1>SocialNework</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ social_nework.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ social_nework.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ social_nework.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_social_network_index') }}">back to list</a>

    <a href="{{ path('app_admin_social_network_edit', {'id': social_nework.id}) }}">edit</a>

    {{ include('admin_social_network/_delete_form.html.twig') }}
{% endblock %}
