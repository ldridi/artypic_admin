{% extends 'baseAdmin.html.twig' %}

{% block title %}New Tags{% endblock %}

{% block body %}

    <div class="content">
        <div class="row">
            <div class="col-md-6">
                <div class="card card-user">
                    <div class="card-header">
                        <h5 class="card-title">Nouveau tag <a href="{{ path('app_admin_tags_index') }}"><button type="button" rel="tooltip" class="btn btn-info btn-icon btn-md " data-original-title="" title="">
                                    <i class="fa fa-home"></i>
                                </button></a></h5>
                    </div>
                    <div class="card-body">
                        {{ include('admin_tags/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
