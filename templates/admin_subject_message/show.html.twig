{% extends 'base.html.twig' %}

{% block title %}SubjectMessage{% endblock %}

{% block body %}
    <h1>SubjectMessage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject_message.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subject_message.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_subject_message_index') }}">back to list</a>

    <a href="{{ path('app_admin_subject_message_edit', {'id': subject_message.id}) }}">edit</a>

    {{ include('admin_subject_message/_delete_form.html.twig') }}
{% endblock %}
