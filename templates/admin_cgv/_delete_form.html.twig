<form method="post" action="{{ path('app_admin_cgu_delete', {'id': cgu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cgu.id) }}">
    <button class="btn">Delete</button>
</form>
