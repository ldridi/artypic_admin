{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<div class="content">
    <div class="row">

        <div class="col-md-8">
            <div class="card card-user">
                <div class="card-header">
                    <h5 class="card-title">Modification du produit "{{ product.title}}" <a href="{{ path('app_admin_product_index') }}"><button type="button" rel="tooltip" class="btn btn-info btn-icon btn-md " data-original-title="" title="">
                                <i class="fa fa-home"></i>
                            </button></a></h5>
                </div>
                <div class="card-body">

                    {{ include('admin_product/_form.html.twig', {'button_label': 'Update'}) }}
                    {{ include('admin_product/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>


{% endblock %}
