{% extends 'baseAdmin.html.twig' %}

{% block title %}Modification Category{% endblock %}

{% block body %}

    <div class="content">
        <div class="row">
            <div class="col-md-6">
                <div class="card card-user">
                    <div class="card-header">
                        <h5 class="card-title" style="color:#EF857D; font-weight: bold">Modification Categorie "{{ category.name}}"  <a href="{{ path('app_admin_category_index') }}"><button type="button" rel="tooltip" class="btn btn-info btn-icon btn-md " data-original-title="" title="">
                                    <i class="fa fa-home"></i>
                                </button></a></h5>
                    </div>
                    <div class="card-body">
                        {{ include('admin_category/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                        {{ include('admin_category/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
