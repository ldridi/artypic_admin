{% extends 'baseAdmin.html.twig' %}

{% block title %}PromoCode{% endblock %}

{% block body %}
    <h1>PromoCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo_code.id }}</td>
            </tr>
            <tr>
                <th>Coupon</th>
                <td>{{ promo_code.coupon }}</td>
            </tr>
            <tr>
                <th>ExpirationDate</th>
                <td>{{ promo_code.expirationDate }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ promo_code.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_promo_code_index') }}">back to list</a>

    <a href="{{ path('app_admin_promo_code_edit', {'id': promo_code.id}) }}">edit</a>

    {{ include('admin_promo_code/_delete_form.html.twig') }}
{% endblock %}
